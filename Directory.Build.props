<Project>

  <PropertyGroup>
    <!--
      VS가 F5(디버깅 시작)/테스트 실행처럼 간접적으로 트리거한 빌드에서
      분석기와 nullable 분석을 자동으로 건너뛰는 최적화를 끈다.
      ("분석기를 건너뛰어 빌드 속도를 높입니다." 메시지의 원인)
    -->
    <OptimizeImplicitlyTriggeredBuild>false</OptimizeImplicitlyTriggeredBuild>
  </PropertyGroup>

</Project>
